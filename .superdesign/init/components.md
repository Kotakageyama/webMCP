# Shared components

This compact Next.js app has no shared `components/` directory. The reusable UI patterns are currently page-local (`TaskSurface`, `SecurityLab`) and are catalogued in `extractable-components.md`.
